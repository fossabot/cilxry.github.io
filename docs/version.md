# 版本文档

> Step To Step 一步一步来

> 没有 deadline 的项目是很可怕的，
> 不要坠入第一个版本还没有开发好就构思之后内容的深渊，
> 不要无限扩展，不要大步子跑。

## 0.0.0-0a

Mar 13

作为我最初应该意识到版本管理问题的一个版本，
当下做的情况其实很半吊子，
但如果这样下去就一定会落入完美主义的黑洞。

### Feat

作为一个最初版本，最不完美的版本，
但它可以挂在互联网上，挂在版本文档的第一页。

- 有一些很基础的页面：首页、归档页、友链页、博客页
- 能查看我写的文章
- 有各种各样的 Styled 卡片（比如首页上的 macOS 样式的卡片）
- 有一个超级牛逼的主题色切换功能
- 有花里胡哨的 frontmatter 系统

没了。

### 已知的 Bug

（其实 bug 有很多我只是不愿示人）

- 在特定情况下 ~~每次~~ 切换页面会导致动画重载
- 忘记给 dark 下的文字换颜色了。

问题还有不少呢。

## 0.0.1-1a

Mar 14

今天免笔试面试，感觉是烷基八氮了。
题目是怎么践行工匠精神，
我用半分钟的时间复述了一遍题目，
半分钟云里雾里的讲怎么做好小事，
讲的很空还不知道人听没听进去。。

我将浑浑噩噩的度过这一周，呜。

在 Rel 第一个版本之后，我发现，
还有很多要补充的呢，比如我发现我写的：

- `i18n`
- 状态监测
- 文档
- 朋友圈
- And any more...

通通是！半吊子。我趣我怎么这么会挖坑

### Bug

- 主题控制的面板写的是绝对距离

### Changes

1. 思索许久之后还是把 `LayoutsComponts` 的东西全部放在 `@cp` 里，不然太费解了
2. 添加了一个 JetBrains Mono Regular 字体，未来考虑怎么添加多字重
3. 接着在这两天依旧是新添加了一些半吊子的东西：

- 隐私协议 \* `docs` 目录下
- 用 AI 写一个代码块可复制的操作，然后它写了个用不了的
- 计划写一个后端，在 `@/config` 下新增了 `backend` 配置项

4. 计划重构 `Styles`，将在未来把散落各地的 `Style` 统整理到 `Styles` 内且尽可能采用 `scss`
5. 在 `astro.config.mjs` 增加 `remark` 和 `rehype` 插件
6. 小配置修改

## 0.0.2-2

Mar 19 - Mar 27

生日快乐。

（几天后）
心烦意乱。

好久没来做更新了，
写一点最近的修改。

### Changes

1. Readme 的小调整，比如说增加了 VSCode 调试描述
2. 给 Gitignore 雕了个花
3. 增加了新的 Package，比如 ExpressiveCode
4. `tsconfig.json` 新增了 `src/config` 的别名
5. 新增了图片灯箱效果，使用 [ViewImage.js](https://tokinx.github.io/ViewImage/) 实现
6. 字体和展示效果小幅修改
7. 修改了 `i18n` 的一些逻辑，现在 `Keys` 会从需要的文件动态生成了
8. 修改了主页面的内容，计划重构
9. 从 `css` 迁移到 `scss`，正在进行中
10. 增加了一个在后端检测过程可能用到的心跳检测

## 0.0.3-3a

Mar 28

简单小优化，
把 `build error` 的问题修复了，
同时知道了 `package.json` 是可以修改版本的。

### Changes

1. 在 `package.json` 修改了版本，引入 `astro check`
2. 移除了不用而且会报错的 `dev` 和 `Layout` 页面
3. i18n 和文档的优化

## 0.0.4

Apr 3

首先先说，这是一个很平平无常的 feature，但是，孩子们我们有 Standard-Version 了

故，此文档将整合到 Standard-Version 生成的版本文档里

End there.
Next step!
