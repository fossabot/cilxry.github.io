---
interface Props {
  cardData: {
    header: string;
    header_eng?: string;
    content?: string;
  };
    clickData?: {
    href: string;
  };
}

const { cardData } = Astro.props;
---

<div
  class="win-card
    w-96 rounded-xl
    /* Mica 效果模拟：浅色用浅灰蓝，深色用深灰 */
    bg-white/90 dark:bg-gray-800/90
    backdrop-blur-xl
    border border-gray-200/60 dark:border-gray-700/60
    shadow-xl
    overflow-hidden
    [box-shadow:0_4px_20px_rgba(0,0,0,0.1)]
    dark:[box-shadow:0_4px_20px_rgba(0,0,0,0.3)]"
>
  <!-- 标题栏 -->
  <div
    class="h-9 px-4 flex items-center justify-between
      bg-white/70 dark:bg-gray-800/70
      border-b border-gray-200/50 dark:border-gray-700/50
      text-sm font-medium"
  >
    <!-- 左侧：图标 + 标题 -->
    <div class="flex items-center gap-2">
      <div
        class="w-5 h-5 rounded-sm bg-blue-500 flex items-center justify-center text-white text-xs"
      >
        {cardData.header[0]}
      </div>
      <span class="text-gray-800 dark:text-gray-200">{cardData.header}</span>
    </div>

    <!-- 右侧：窗口控制按钮 -->
    <div class="flex gap-1">
      <div
        class="w-8 h-6 flex items-center justify-center rounded hover:bg-gray-200/60 dark:hover:bg-gray-600/50 cursor-pointer"
      >
        <div class="w-4 h-0.5 bg-gray-500 dark:bg-gray-400 rounded-full"></div>
      </div>
      <div
        class="w-8 h-6 flex items-center justify-center rounded hover:bg-gray-200/60 dark:hover:bg-gray-600/50 cursor-pointer"
      >
        <div
          class="w-4 h-4 border border-gray-500 dark:border-gray-400 rounded-sm"
        >
        </div>
      </div>
      <div
        class="win-close
          w-8 h-6 flex items-center justify-center rounded
          hover:bg-red-500/20 cursor-pointer"
      >
        <div
          class="w-4 h-0.5 bg-gray-500 dark:bg-gray-300 rounded-full rotate-45 origin-center"
        >
        </div>
        <div
          class="w-4 h-0.5 bg-gray-500 dark:bg-gray-300 rounded-full -rotate-45 origin-center absolute"
        >
        </div>
      </div>
    </div>
  </div>

  <!-- 内容区 -->
  <div class="p-5">
    <h2 class="text-lg font-semibold text-gray-900 dark:text-gray-100 mb-2">
      {cardData.header}
    </h2>
    <p class="text-sm text-gray-700 dark:text-gray-300 leading-relaxed">
      这是模仿 Windows 11 的 Mica 材质窗口，带有亚克力模糊、直角标题栏和 Fluent
      Design 控制按钮。
    </p>
  </div>
</div>

<style scoped>
  /* body {
    font-family: "Segoe UI", system-ui, sans-serif;
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 100vh;
    margin: 0;
    padding: 2rem;
    background: linear-gradient(135deg, #d0d0d0, #f0f0f0);
  } */

  .win-card {
    will-change: transform, box-shadow;
    transition:
      transform 0.2s ease,
</style>
